global:
  scrape_interval: 15s  # Scrape metrics every 15 seconds

scrape_configs:
  - job_name: 'nodejs-app'
    static_configs:
      - targets: ['13.59.152.118:3000']  # IP and port of your Node.js app (replace with the actual IP)

